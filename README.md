Dockerfile_IACAgent : Jenkins ssh agent which contains awscli, terraform, and ansible installed in.
Jenkinsfile_IACAgent : Basic version check in Jenkins pipeline to confirm the aws, terraform and ansible installation
